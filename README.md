# Matsu RallyCon v0.8.43
UNIVERSAL PDF + A5 RALLY VIEW / 逆走リカバリーモード

## v0.8.43 REV表示の最終配置調整
- v0.8.42を基準に、右下のAUTO/REV操作列だけを5px上へ移動。下部フッターとの干渉を避ける。
- REV戻り距離の表示位置・サイズはv0.8.42を維持。
- REV開始直後の戻り距離表示を「−0.00」ではなく「0.00」とし、0.005km未満は0.00表示。実際に戻った場合は「−0.01」のように表示する。
- 逆走距離計算、TOTAL減算、AUTO NEXT停止、RB/コマ図の自動復帰なし、GPS処理は変更しない。
- A5中央表示位置、PDF、その他の既存UI・操作は変更しない。

### v0.8.43 検証
- v0.8.42を基準に、REV表示更新式、REV/AUTO配置CSS、title/APP_VERSION/VERSION.txt/READMEのバージョン表記のみ変更。
- 埋め込みA5 PDFデータがv0.8.42と一致することを確認。
- 逆走処理本体（距離計算・TOTAL減算・AUTO NEXT停止・GPS処理）の差分がないことを確認。
- JavaScript構文チェック、ZIP整合性、ファイル構成を確認。

# Matsu RallyCon v0.8.42
UNIVERSAL PDF + A5 RALLY VIEW / 逆走リカバリーモード

## v0.8.42 REV戻り距離の配置調整
- v0.8.41を基準に、REV戻り距離の表示位置だけを調整。
- REV戻り距離をAUTO/REVボタンの上に小さく配置し、ボタンと重ならないようにした。
- 通常時はREV戻り距離を非表示、REV ON時だけ表示する既存仕様を維持。
- AUTOとREVの横並び配置、REV ON時の黄色表示は変更しない。
- 逆走時のTOTAL減算、REV戻り距離の計算、AUTO NEXT停止、コマ図/RBの自動復帰なし、GPS処理は変更しない。
- A5中央表示位置、PDF、GPS、その他の既存UI・操作は変更しない。

### v0.8.42 検証
- v0.8.41を基準に、REV表示DOM配置とREV/AUTO周辺CSS、およびバージョン表記のみ変更。
- 逆走処理JavaScript本体に変更がないことを差分確認。
- 埋め込みPDFデータがv0.8.41と一致することを確認。
- JavaScript構文チェック、ZIP整合性、HTML / VERSION.txt / READMEのバージョン表記を確認。

# Matsu RallyCon v0.8.41
UNIVERSAL PDF + A5 RALLY VIEW / 逆走リカバリーモード

## v0.8.41 REVボタン配置調整
- v0.8.40の逆走機能・GPS処理・TOTAL/REV戻り距離・RB/コマ図・AUTO NEXTの動作は変更しない。
- 右側パネル下部のAUTOとREVを同一行に横並び配置。
- AUTOを主ボタンとして広く確保し、REVは頻繁に使わない補助機能として幅78pxに限定。
- REVが下部フッターに隠れないよう、右側パネル内のナビゲーション領域だけを調整。
- 横画面の既存レイアウト、A5中央表示、PDF、GPS、その他の操作は変更しない。

### v0.8.41 検証
- v0.8.40のindex.htmlを基準に、REV/AUTO配置用CSSとバージョン表記のみ変更。
- 逆走処理のJavaScript本体は変更しない。
- 埋め込みPDFデータは変更しない。
- HTML / VERSION.txt / READMEのバージョンをv0.8.41へ統一。

# Matsu RallyCon v0.8.40
UNIVERSAL PDF + A5 RALLY VIEW / 逆走リカバリーモード

## v0.8.40 逆走モード（REV）
- v0.8.39を基準に、通常走行時のGPS距離計算・コマ進行を維持。
- 走行中かつGPS ONのときだけREVを手動でON/OFFできる。
- REV ON中は、GPSで実際に移動した距離を補正率適用後にTOTALから減算する。
- REV ON中の減算距離を「REV 戻り距離」として別表示する。
- コマ図のRBは自動では戻さない。AUTO NEXTもREV中は停止する。
- ライダーが実際に戻った地点を確認した後、既存のBACK操作で必要なコマへ戻す。
- REV OFF後は、その時点のTOTALから通常のGPS距離加算へ復帰する。
- REVを使用しない通常走行では、従来のGPS距離処理・AUTO NEXT・コマ図進行に影響しない。

### v0.8.40 検証
- v0.8.39をベースに逆走処理を独立追加。A5中央表示、PDF解析、PDF表示、NEXT/BACK、GPS補正、通常AUTO NEXTの既存ロジックは変更しない。
- REV中のGPS通常区間とGPS LOST復帰区間の両方で距離を減算する処理を確認。
- REV中はAUTO NEXTが動作しないことを確認。
- REV OFF時に通常加算へ復帰することを確認。
- 埋め込みPDFは変更しない。
- HTML / VERSION.txt / READMEのバージョンをv0.8.40へ統一。


## v0.8.39 A5コマ練：中央下寄せのパン上限を修正
- v0.8.38の表示位置設定（全コマ10px下、1コマ目のみ追加5px下）は変更しない。
- `panBounds()` 側にも同じ下方向補正量を反映し、`clampPan()` によって補正量が削られないよう修正。
- 1コマ目は合計15px下、2～6コマ目は10px下を実際に許容する。
- PDF、距離解析、RB判定、NEXT/BACK、AUTO NEXT、GPS、その他UIは変更しない。

### v0.8.39 検証
- v0.8.38との差分はA5 `panBounds()` の下方向補正許容処理とバージョン表記のみ。
- `centerTargetInView()` の表示位置ロジックは変更なし。
- 埋め込みPDFデータは不変。
- HTML / VERSION.txt / READMEのバージョンをv0.8.39へ統一。

# Matsu RallyCon v0.8.38
UNIVERSAL PDF + A5 RALLY VIEW / 電動マップホルダー風連続スクロール試作

## v0.8.38 A5コマ練：1コマ目だけ中央表示を追加調整
- v0.8.37を基準として、A5コマ練の現在RB中央表示位置だけを調整。
- v0.8.37の全コマ **10px下** は維持。
- 各A5ページの **1コマ目（row 1）のみ追加5px下** とし、合計15px下へ調整。
- 2～6コマ目はv0.8.37と同じ位置を維持。
- PDF、倍率、距離解析、RB判定、NEXT/BACK、AUTO NEXT、GPS、PDF表示モード、連続スクロール等は変更しない。
- v0.8.36で追加したページ端の中央化許容処理も維持。

### v0.8.38 検証
- v0.8.37からの表示Y位置変更はA5 `centerTargetInView()` の1箇所のみ。
- 1コマ目だけ追加5px、2～6コマ目は従来の10px下を維持することを静的確認。
- HTML / VERSION.txt / READMEのバージョン表記をv0.8.38へ更新。
- JavaScript構文チェック、ZIP整合性、v0.8.37との差分、埋め込みPDFデータ不変を確認。

# Matsu RallyCon v0.8.36
UNIVERSAL PDF + A5 RALLY VIEW / 電動マップホルダー風連続スクロール試作

## v0.8.36 A5コマ練：ページ端のコマを中央表示
- v0.8.35を基準に、A5コマ練の現在RBを中央へ合わせる既存処理を維持。
- ページ最上段／最下段のRBでは、中央化に必要な範囲だけPDFページ外への上下移動を許可。
- ページ外にはPDFを描画せず、既存のステージ背景（暗色）が見える。
- PDF内容、倍率、距離解析、RB判定、NEXT/BACK、AUTO NEXT、GPS、PDF表示モード、電動マップホルダー風連続スクロールは変更しない。
- 横画面A5コマ練の表示位置制御が対象。縦RALLY VIEWの連続表示ロジックには変更なし。

### v0.8.36 検証
- v0.8.35からの機能差分を `panBounds()` のA5上下許容処理1箇所に限定。
- JavaScript構文チェック、差分確認、埋め込みPDFデータのSHA-256一致を確認。
- 実機ではページ上端／下端のRBを重点確認し、NEXT/BACK、AUTO NEXT、長押し等の副作用がないことを確認する。

## 変更点
- v0.8.24の縦RALLY VIEW、PDF表示モード縦RALLY VIEW、送り補正を維持。
- 横画面の連続スクロールでBACKが前ページへ移動した際、前ページの先頭へジャンプせず、前ページ下端から再開するよう修正。
- START後のPDF表示モード（LONGPDF / PDFVIEW）で、NEXT/BACKを長押しすると電動マップホルダー風の連続スクロールを開始。
- 長押ししている間だけ連続してスライドし、離すとその位置で停止。
- ゆっくり動き始めて短時間で速度が上がる、緩やかな加速式。
- 短押しは従来どおり1回送り。
- 縦RALLY VIEWでは連結されたPDF全体を連続スクロール。
- 横画面では従来のPDF表示領域を連続スクロールし、表示単位境界では既存のページ移動処理を利用。
- START前のNEXT/BACK長押しによる「PDFコマ送り量」設定は従来どおり。
- A5コマ練モードのNEXT/BACK長押しは今回変更せず、従来の1コマ送りを維持。
- v0.8.24の縦ヘッダー表示、GPS、既存PDF表示、PDFタップ/ピンチ/ダブルタップ等を維持。


## v0.8.35 起動時の標準コマ図をA5版なのはなラリーへ変更
- v0.8.34をベースに、アプリ起動時に自動読み込みする埋め込みPDFだけを変更。
- 従来の内蔵「コマ練Classic2026」A4 PDFを、ユーザー提供の「【コマ図（A5）】第6回なのはなラリー」A5 PDFへ置換。
- 起動時のA5 PDFは既存のA5コマ練自動解析を通し、11ページ・66RBのロードブックとして読み込む構成。
- 表紙ページ（PDF 1ページ）は解析対象外で、ロードブック1ページ目（PDF 2ページ）から開始する既存A5マッピングを維持。
- TOTAL / INTER / RB、NEXT/BACKなどのA5コマ練機能は既存処理を使用し、新しいPDF専用の距離データをハードコードしない。
- LOAD PDFでユーザーが別PDFを読み込む通常機能には変更なし。
- v0.8.34で実機確認済みの縦PDFレイアウト、横画面、ウィーン連続スクロール、GPS等の処理は変更しない。

### v0.8.35 検証
- 埋め込みPDFの元ファイルSHA-256と、HTML内base64を復号したデータが一致することを確認。
- 埋め込みPDFが12ページ、A5判（419.52 x 595.32 pt）、暗号化なしであることを確認。
- A5 PDFのロードブックページはPDF 2～12ページ、各6RB、合計66RBであることを資料から確認。
- HTML / VERSION.txt / READMEのバージョン表記をv0.8.35へ統一。
- インラインJavaScriptの構文チェックを実施。
- v0.8.34からの差分を確認し、起動時埋め込みPDFとバージョン情報以外の機能ロジックに意図した変更がないことを確認。
- 実機でのv0.8.35起動時A5コマ図表示・距離解析は未確認。

## v0.8.29の修正
- NEXTで次ページへ移動するときは次ページ上端から継続。
- BACKで前ページへ移動するときは前ページ下端から継続。
- これにより横画面でも巻き取り方向に対してページ境界のジャンプ感を抑制。
- v0.8.27で確認済みの巻き取り速度（55～160px/s）は変更なし。

## 連続スクロール試作の操作
### 走行前
NEXT/BACK長押し（約0.7秒） -> 「PDFコマ送り量」設定

### START後 / PDF表示モード
短押し -> 従来どおり1回送り
長押し -> 電動マップホルダー風に連続スクロール
離す -> その位置で停止

## 対象
- LONGPDF
- PDFVIEW
- PDF表示モードの縦RALLY VIEW / 横画面

A5コマ練の連続スクロールは今回まだ対象外。既存のA5コマ練動作を優先して保持している。

## 検証
- index.html内のインラインJavaScript 3ブロックを抽出して node --check で構文確認。
- v0.8.24の配布ZIPを基準に差分確認し、意図した連続スクロール処理・バージョン更新以外の変更がないことを確認。
- NEXT/BACKの長押し処理がPDFモード選択後にも確実に有効になる構造を確認。
- START前は既存設定長押し、START後は連続スクロールへ切り替わる分岐を確認。
- 縦/横の連続スクロール方向、停止、短押しの分岐を静的確認。
- VERSION.txt / README / HTMLのバージョン表記を一致確認。
- ZIP整合性を確認。
- iPhone実機でのv0.8.27連続スクロール確認は未実施。まずPCで操作感を確認するための試作版。


## v0.8.27 PCテスト修正
- PDF表示モードの横画面でも長押し連続スクロールが表示単位の境界を越えて継続するよう修正。
- 横画面でページ高さが表示領域より短い場合も、端で論理表示単位へ進めるよう端判定を修正。
- 電動マップホルダー風の連続スクロール速度を v0.8.25 より低速化（約85～250px/s）。
- 長押し開始時の加速は約1.5秒で緩やかに一定速度へ移行。
- A5コマ練の既存NEXT/BACK、START、横画面の通常1コマ送り、PDFコマ送り設定、縦RALLY VIEWの補正値などは変更しない。
- JS構文チェックおよびv0.8.25との差分確認を実施。実機テストは未実施。


## v0.8.27 変更
- 電動マップホルダー風の連続巻き取り速度をさらに低速化。
- 速度カーブを 55～160 px/s に変更（従来 85～250 px/s）。
- NEXT/BACK の連続スクロール開始・停止、既存のSTART前設定長押し、A5縦RALLY VIEW、横画面動作は変更なし。
- 変更は巻き取り速度とバージョン情報に限定。


## v0.8.29変更点
- 横画面・縦画面とも、走行画面上の常時バージョン表示を非表示化。
- Matsu RallyConのブランド名、時刻、GPS、CAP等の既存表示は維持。
- 機能・操作ロジックは変更なし。


## v0.8.33 縦PDF読み込みモードの表示領域固定・ボタン再配置
- v0.8.32をベースに、縦画面のPDF読み込みモード（LONGPDF / PDFVIEW）の表示領域を調整。
- TOTAL / SPEEDの枠下端より上にはPDFが表示されないよう、PDFスクロール領域の上端をTOTAL / SPEED HUDの下端へ固定。
- PDFの表示倍率・横幅は変更しない。
- 最初のPDFページは、PDF表示領域の下端に下端を合わせて表示する。START後もこの配置を維持し、論理ページ同期による上方向へのずれを防止。
- 縦PDF読み込みモードではPAGE ‹ / PAGE ›ボタンを非表示。
- 下部ボタンは左列をLOAD PDF / START(STOP)、中央列をBACK、右列をNEXTとし、BACK/NEXTを上下2段分の大型ボタンとして配置。
- 横画面、A5コマ練、GPS、PDFコマ送り率、縦PDF送り補正、ウィーン連続スクロールの処理は変更しない。

### v0.8.33 検証
- v0.8.32のZIPをベースに変更。
- 縦PDFモードのCSS、START時の表示処理、PDF表示領域固定処理、下部ボタン配置のみ変更。
- 横画面の連続スクロール処理およびv0.8.31の長押し停止時クリック抑制処理は変更なし。
- JavaScript構文チェックを実施。
- 実機での縦画面レイアウトは未確認。

## v0.8.32 縦PDF読み込みモードの表示レイアウト調整
- v0.8.31をベースに、縦画面のPDF読み込みモード（LONGPDF / PDFVIEW）の表示レイアウトだけを変更。
- 縦PDF読み込みモードではRB表示を非表示化。スライド式PDF表示ではRB番号を常時表示する必要がないため、その領域を使用。
- TOTAL / SPEEDを2分割の大型表示へ変更し、走行中の視認性を優先。
- PDFの表示倍率・横幅は変更せず、最初のPDF 1枚の下端が縦RALLY VIEWの表示領域下端に合うよう、上側の余白を動的に調整。
- これにより従来PDF上に重なっていた上側の空間をTOTAL / SPEED表示に有効利用。
- 横画面のレイアウト、A5コマ練モード、NEXT/BACKのウィーン連続スクロール、離した瞬間の停止処理は変更しない。
- PDFコマ送り％、縦PDF送り補正、PDFの自由スクロールも変更しない。

### v0.8.32 検証
- v0.8.31のHTMLをベースに、縦PDF読み込みモードのCSS・表示構築処理のみを変更。
- インラインJavaScriptブロックを抽出してnode --checkで構文確認。
- バージョン表記（HTML / VERSION.txt / README）を一致させる。
- 横画面の連続スクロール処理、NEXT/BACKのクリック抑制処理に差分がないことを確認。
- 実機での表示サイズ・PDF下端位置は未確認。

## v0.8.30 GPS LOST復帰表示の即時化
- v0.8.29をベースに、GPS LOST→有効なGPS復帰時の表示経路だけを変更。
- 復帰GPSで補正距離を計算した直後に、GPSステータスとTOTAL/LEG等を即時更新。
- 復帰時の表示更新ではlocalStorageへのセッション保存を待たず、ブラウザの描画を優先。
- セッション保存は表示更新後の次のタスクへ延期し、復帰表示までの同期処理を短縮。
- GPS距離の推定方法、GPS LOST判定、復帰補正の計算式、速度推定による補完は変更なし。
- 二重加算防止のため、復帰補正後に現在位置を新しいGPS基準点として更新。
- A5コマ練、PDF表示、RALLY VIEW、NEXT/BACK、電動マップホルダー風連続スクロール等は変更なし。

### v0.8.30 検証
- v0.8.29からの差分を確認し、GPS復帰表示経路以外に意図した変更がないことを確認。
- インラインJavaScriptの構文チェックを実施。
- 実機走行でのGPS LOST→復帰表示時間は未確認。v0.8.30はこの点を実走テストするための版。


## v0.8.31 横画面PDF連続スクロールの停止時ジャンプ修正
- v0.8.30をベースに、横画面PDF読み込みモードのNEXT/BACK長押し終了時だけを修正。
- 長押し中に電動マップホルダー風連続スクロールが開始された場合、ボタンを離した直後のブラウザ合成clickを通常のNEXT/BACKとして処理しないよう修正。
- これにより、ボタンを離した瞬間にPDFコマ送り率の通常送りが追加発動して、一気にページ/表示位置がジャンプする現象を防止。
- 長押し中の連続スクロール自体、停止位置、スクロール速度、ページ境界処理は変更なし。
- 短押しは従来どおり1回送り。
- START前の長押しによる「PDFコマ送り量」設定も変更なし。
- 縦RALLY VIEW、A5コマ練、GPS、その他の既存機能は変更なし。

### v0.8.31 検証
- v0.8.30から対象イベント処理の差分を確認。
- 長押し開始後はpointerup時に抑制フラグを解除せず、後続onclickがフラグを消費して終了することを確認。
- 短押しでは抑制フラグが立たず、既存の1回送り処理が実行されることを確認。
- インラインJavaScriptの構文チェックを実施。
- 実機での横画面PDF長押し停止時ジャンプは、v0.8.31での走行/操作テストが必要。


## v0.8.34 縦PDF読み込み時の初期ページ修正
・縦PDF読み込みモードでPDFを読み込んだ直後、先頭ページから開始するよう修正しました。
・v0.8.33では、1ページが表示領域より縦長の場合に全体のスクロール最大値を初期位置へ使用していたため、読み込み直後に最終ページまでジャンプする場合がありました。
・v0.8.34では初期スクロール位置を常に0（先頭ページ）に固定します。
・TOTAL / SPEEDの下端をPDF表示領域の上端とする固定境界は維持します。
・縦PDFの大型TOTAL/SPEED、RB非表示、LOAD/START・大型BACK/NEXT配置、自由スクロール、NEXT/BACK長押しのウィーン連続スクロールは変更していません。
・横画面、A5コマ練、GPS、その他の既存機能は変更していません。

### v0.8.34 検証
・対象コード差分を確認
・JavaScript構文チェックを実施
・実機でのPDF読み込み確認は未実施
